{
  "name": "node-mac-permissions-reproduction",
  "version": "1.0.0",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "postinstall": "electron-builder install-app-deps",
    "start": "electron .",
    "build": "NODE_ENV='production' && electron-builder build -m -c.mac.identity=null"
  },
  "devDependencies": {
    "electron": "^25.2.0",
    "electron-builder": "^24.4.0",
    "node-mac-permissions": "^2.2.1"
  },
  "optionalDependencies": {
  }
}
